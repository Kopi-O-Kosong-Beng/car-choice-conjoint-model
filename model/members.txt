# Models in the production blend. One name per line; each needs an
# oof_<name>.rds / test_<name>.rds pair in model/artifacts/.
# This set produced submissions/sub_20260726_0651.csv at nested 1.13044.
mnl_pw        # part-worth conditional logit      1.15686  weight 0.000 (displaced by lcmnl3)
xgb_lw2       # listwise xgboost, tuned           1.14152  weight 0.216
xgb_mono      # + monotone price constraint       1.13980  weight 0.337
lcmnl3        # latent-class MNL, 3 classes       1.14396  weight 0.447
# xgb_resenc2 # leak-free residual encoding       1.13721  -- HELD BACK, see EXPERIMENTS.md
#               blend with it reaches 1.12825, but its leak-freeness rests on an
#               argument rather than a construction. Pending the nested double-OOF test.
